# http://rust-ci.org/help/
# http://docs.travis-ci.com/user/languages/rust/
language: rust
install:
  - sudo apt-get install -qq protobuf-compiler
script:
  - cargo build
  - export PATH=./target:$PATH
  - make regenerate
  - cargo test
